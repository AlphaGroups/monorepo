
name: Deploy LMS App to Azure

on:
  push:
    branches:
      - main  # or your production branch

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    # 👇 This ensures all yarn commands run inside apps/lms
    defaults:
      run:
        working-directory: apps/lms

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: "yarn"

      - name: Install dependencies
        run: yarn install --frozen-lockfile

      - name: Build Next.js LMS app
        run: yarn build

      - name: List build output
        run: ls -la .next

      - name: Zip build artifacts
        run: |
          zip -r ../../release.zip .next node_modules package.json yarn.lock public next.config.* tsconfig.* .env.production

      - name: Deploy to Azure App Service
        uses: azure/webapps-deploy@v3
        with:
          app-name: alpha-lms-app
          publish-profile: ${{ secrets.AZURE_LMS_PUBLISH_PROFILE }}
          package: ./release.zip
